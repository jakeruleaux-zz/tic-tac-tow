import c from './../../src/constants';
import tictac from './../../src/reducers/tictac-reducer';

describe('TicTacToe Reducer', () => {
  let action;


  test('do some stuff', () => {
    expect()
  })
})

/* specs

1. when you click, the box fills with an X
2. when you click, switch turns
3. when someone wins, announce a winner.
4. when you click, the move is recorded in history on screen
